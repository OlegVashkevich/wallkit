# Компонент Button

Универсальный компонент кнопки для действий в формах и интерфейсе. Реализует HTML-элементы `<button>` и `<a>` с полной типобезопасностью на PHP и кастомизацией через CSS-переменные.

## Характеристики

| Параметр | Значение                              |
|----------|---------------------------------------|
| **Тип компонента** | Элемент интерфейса                    |
| **Иммутабельность** | Да (readonly)                         |
| **Рендеринг** | SSR (серверный)                       |
| **Типизация** | Strict PHP 8.2+                       |
| **CSS префикс** | `wallkit-`                            |
| **Зависимости** | Base, WithHelpers, WithStrictHelper, WithInheritance |

## Поддерживаемые типы кнопок

| Тип | Назначение | HTML-тег |
|-----|------------|-----------|
| `button` | Обычная кнопка | `<button type="button">` |
| `submit` | Отправка формы | `<button type="submit">` |
| `reset` | Сброс формы | `<button type="reset">` |
| `link` | Кнопка-ссылка | `<a href="...">` |

## Варианты стилей

| Вариант | Назначение |
|---------|------------|
| `primary` | Основное действие |
| `secondary` | Второстепенное действие |
| `success` | Успешное действие |
| `danger` | Опасное действие |
| `warning` | Предупреждение |
| `info` | Информационное |
| `light` | Светлый вариант |
| `dark` | Тёмный вариант |
| `link` | Стиль ссылки |

## Свойства компонента

| Свойство | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `text` | `string` | — | Текст кнопки (обязательно) |
| `type` | `string` | `button` | Тип кнопки |
| `variant` | `string` | `primary` | Вариант стиля |
| `size` | `string` | `md` | Размер (sm, md, lg) |
| `disabled` | `bool` | `false` | Отключенное состояние |
| `icon` | `?string` | `null` | Иконка перед текстом |
| `iconAfter` | `?string` | `null` | Иконка после текста |
| `href` | `?string` | `null` | Ссылка (рендерит как `<a>`) |
| `target` | `?string` | `null` | Цель ссылки |
| `fullWidth` | `bool` | `false` | На всю ширину |
| `outline` | `bool` | `false` | Контурный вариант |
| `rounded` | `bool` | `false` | Закруглённые углы |
| `onClick` | `?string` | `null` | JavaScript обработчик onclick |

## Использование

### Базовое использование
```php
use OlegV\WallKit\Form\Button\Button;

$button = new Button(
    text: 'Нажми меня'
);
echo $button;
```

### Кнопка отправки формы
```php
$submitButton = new Button(
    text: 'Отправить',
    type: 'submit',
    variant: 'success',
    size: 'lg'
);
```

### Кнопка-ссылка
```php
$linkButton = new Button(
    text: 'Перейти в профиль',
    href: '/profile',
    variant: 'link',
    target: '_blank'
);
```

### Кнопка с иконкой
```php
$deleteButton = new Button(
    text: 'Удалить',
    icon: '🗑️',
    variant: 'danger',
    size: 'sm'
);
```

## Стилизация

### CSS-классы
| Класс | Назначение |
|-------|------------|
| `.wallkit-button` | Базовый стиль кнопки |
| `.wallkit-button--{variant}` | Стиль по варианту |
| `.wallkit-button--outline-{variant}` | Контурный вариант |
| `.wallkit-button--{size}` | Размер кнопки |
| `.wallkit-button--disabled` | Отключенное состояние |
| `.wallkit-button--full-width` | На всю ширину |
| `.wallkit-button--rounded` | Закруглённые углы |
| `.wallkit-button__icon` | Стиль иконки |
| `.wallkit-button__text` | Стиль текста |

### CSS-переменные
Компонент использует переменные из базовой темы:
- `--wk-color-*` — цвета для вариантов
- `--wk-spacing-*` — отступы и размеры
- `--wk-font-*` — типографика
- `--wk-border-radius-*` — радиусы скругления
- `--wk-shadow-*` — тени и эффекты
- `--wk-transition-*` — анимации

## Совместимость

| Система | Статус | Примечания |
|---------|--------|------------|
| **PHP** | 8.2+ | Strict типизация, readonly классы |
| **HTML** | 5+ | Семантическая разметка |
| **CSS** | 3+ | CSS-переменные, Flexbox |
| **JavaScript** | ES6+ | Прогрессивное улучшение |

## Ссылки
- [Базовый класс Base](../Base/README.md)
- [Компонент Input](../Input/README.md)
- [Документация по формам](../../Form/Form/README.md)

---

**Версия:** 1.0.0  
**Автор:** OlegV  
**Лицензия:** MIT
