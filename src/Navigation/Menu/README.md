# Компонент Menu

Универсальный компонент меню для навигации и действий. Поддерживает все варианты: горизонтальное (navbar), вертикальное (sidebar), выпадающее (dropdown) и контекстное (context).

## Характеристики

| Параметр | Значение |
|----------|----------|
| **Тип компонента** | Универсальное меню |
| **Иммутабельность** | Да (readonly) |
| **Рендеринг** | SSR (серверный) |
| **Типизация** | Strict PHP 8.2+ |
| **CSS префикс** | `wallkit-` |
| **Адаптивность** | Mobile-first |

## Свойства компонента

| Свойство | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `items` | `array` | `[]` | Элементы меню |
| `orientation` | `string` | `horizontal` | Ориентация (horizontal\|vertical) |
| `variant` | `string` | `navbar` | Вариант (navbar\|sidebar\|dropdown\|context) |
| `position` | `string` | `top` | Позиция (top\|left\|right\|bottom\|floating) |
| `trigger` | `string` | `always` | Триггер показа (always\|hover\|click\|context) |
| `brand` | `?string` | `null` | Текст бренда |
| `collapsible` | `bool` | `false` | Сворачиваемое меню |
| `searchPlaceholder` | `?string` | `null` | Плейсхолдер поиска |
| `maxNestingLevel` | `int` | `0` | Макс. уровень вложенности (0 = без ограничений) |

## Использование

### Navbar (горизонтальное меню)
```php
use OlegV\WallKit\Navigation\Menu\Menu;

$menu = new Menu(
    items: [
        ['label' => 'Главная', 'url' => '/', 'active' => true],
        ['label' => 'О нас', 'url' => '/about'],
        ['label' => 'Контакты', 'url' => '/contact'],
    ],
    variant: 'navbar',
    brand: 'МойСайт',
    searchPlaceholder: 'Поиск...',
);
echo $menu;
```

### Sidebar (вертикальное меню)
```php
$menu = new Menu(
    items: [
        [
            'label' => 'Дашборд',
            'icon' => '📊',
            'url' => '/dashboard',
            'active' => true,
        ],
        [
            'label' => 'Пользователи',
            'icon' => '👥',
            'children' => [
                ['label' => 'Список', 'url' => '/users'],
                ['label' => 'Добавить', 'url' => '/users/new'],
            ],
        ],
    ],
    variant: 'sidebar',
    orientation: 'vertical',
    collapsible: true,
);
```

### Dropdown (выпадающее меню)
```php
$menu = new Menu(
    items: [
        ['label' => 'Редактировать', 'action' => 'edit'],
        ['label' => 'Удалить', 'action' => 'delete', 'danger' => true],
    ],
    variant: 'dropdown',
    trigger: 'click',
);
```

### Context (контекстное меню)
```php
$menu = new Menu(
    items: [
        ['label' => 'Копировать', 'action' => 'copy'],
        ['label' => 'Вставить', 'action' => 'paste'],
    ],
    variant: 'context',
    trigger: 'context',
);
```

## Структура элементов

```php
$items = [
    [
        'label' => 'Текст',           // Обязательно
        'url' => '/path',             // Для ссылок
        'action' => 'actionName',     // Для действий (взаимозаменимо с url)
        'icon' => '📊',               // Иконка emoji
        'active' => true,             // Активное состояние
        'danger' => false,            // Опасное действие
        'disabled' => false,          // Отключено
        'children' => [               // Вложенные элементы
            ['label' => 'Подпункт', 'url' => '/sub'],
        ],
    ],
];
```

## JavaScript API

```javascript
// Инициализация всех меню на странице
WallKitMenu.init();

// Инициализация конкретного меню
const menu = document.querySelector('.wallkit-menu');
WallKitMenu.initMenu(menu);

// Открытие подменю
WallKitMenu.openSubmenu('.wallkit-menu', '.wallkit-menu__item:first-child');

// Закрытие подменю
WallKitMenu.closeSubmenu('.wallkit-menu', '.wallkit-menu__item:first-child');

// Переключение мобильного меню
WallKitMenu.toggleMenu('.wallkit-menu');
```

## События

Компонент генерирует события:
- `wallkit:menu:submenu:open` — при открытии подменю
- `wallkit:menu:submenu:close` — при закрытии подменю
- `wallkit:menu:toggle` — при переключении мобильного меню

## Совместимость

| Система | Статус | Примечания |
|---------|--------|------------|
| **PHP** | 8.2+ | Strict типизация |
| **HTML** | 5+ | Семантическая разметка, ARIA |
| **CSS** | 3+ | CSS-переменные, Flexbox, Grid |
| **JavaScript** | ES6+ | Прогрессивное улучшение |

---

**Версия:** 1.0.0  
**Автор:** OlegV  
**Лицензия:** MIT
