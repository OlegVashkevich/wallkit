# 🧩 Markdown

Компонент для рендеринга Markdown в HTML с использованием библиотеки [Parsedown](https://github.com/erusev/parsedown).

## 📋 Зависимости

Для работы компонента требуется установка библиотеки Parsedown:

```bash
composer require erusev/parsedown
```

Для дополнительных функций (таблицы, сноски и т.д.) рекомендуется установить ParsedownExtra:

```bash
composer require erusev/parsedown-extra
```

## 📋 Свойства (Properties)

| Свойство | Тип | Обязательное | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `content` | `string` | Да | - | Markdown-контент для рендеринга |
| `safeMode` | `bool` | Нет | `true` | Безопасный режим (экранирует HTML) |
| `useExtra` | `bool` | Нет | `false` | Использовать ParsedownExtra если установлен |
| `options` | `array` | Нет | `[]` | Дополнительные опции парсера |

## 🎯 Использование

### Базовый пример
```php
use OlegV\WallKit\Content\Markdown\Markdown;

$markdown = new Markdown(
    content: '# Заголовок\n\nЭто **жирный** текст и [ссылка](https://example.com).'
);

echo $markdown;
```

### С ParsedownExtra (таблицы, сноски и т.д.)
```php
$markdown = new Markdown(
    content: '# Документ с таблицей\n\n| Заголовок | Данные |\n|-----------|--------|\n| Ячейка 1 | Ячейка 2 |',
    useExtra: true
);
```

### Быстрый рендеринг (статический метод)
```php
// Рендеринг блока
$html = Markdown::render('# Заголовок\nТекст');

// Рендеринг инлайн (без блоковых элементов)
$inline = Markdown::renderInline('**жирный** текст');
```

### Опции парсера
```php
$markdown = new Markdown(
    content: $content,
    options: [
        'setBreaksEnabled' => true,  // Включить переносы строк
        'setMarkupEscaped' => false, // Отключить экранирование разметки
        'setUrlsLinked' => true,     // Автоматические ссылки
    ]
);
```

## ⚠️ Безопасность

Компонент использует безопасный режим по умолчанию (`safeMode: true`), который:

1. Экранирует HTML теги
2. Фильтрует опасные URL в ссылках и изображениях
3. Предотвращает XSS-атаки

**Важно:** Отключайте `safeMode` только для доверенного контента!

```php
// НЕ ДЕЛАЙТЕ ЭТО для пользовательского ввода!
$dangerous = new Markdown(
    content: $_POST['user_content'],
    safeMode: false // ⚠️ ОПАСНО!
);

// Всегда используйте безопасный режим для пользовательского ввода
$safe = new Markdown(
    content: $_POST['user_content'],
    safeMode: true // ✅ БЕЗОПАСНО
);
```

## ✨ Возможности

### Поддерживаемый синтаксис (с ParsedownExtra):
- **Заголовки** (#, ##, ###)
- **Форматирование**: жирный, курсив, зачеркивание
- **Списки**: упорядоченные и неупорядоченные
- **Ссылки и изображения**
- **Код**: инлайн и блоки с указанием языка
- **Цитаты**
- **Горизонтальные линии**
- **Таблицы**
- **Сноски**
- **Определения**
- **Аббревиатуры**

## 🎨 Кастомизация

### CSS Стили
Компонент использует CSS-переменные WallKit:

```css
.wallkit-markdown {
    --wk-color-primary: #3b82f6;    /* Цвет ссылок */
    --wk-color-border: #e5e7eb;     /* Границы */
    --wk-color-bg-secondary: #f9fafb; /* Фон блоков кода */
}
```

### Настройка парсера
Вы можете передать любые публичные методы Parsedown через опции:

```php
$markdown = new Markdown(
    content: $content,
    options: [
        'setBreaksEnabled' => true,  // Преобразовывать переносы строк в <br>
        'setUrlsLinked' => false,    // Не преобразовывать URL в ссылки
        'setMarkupEscaped' => true,  // Экранировать HTML разметку
    ]
);
```

## 🧪 Примеры

### Чтение из файла
```php
$content = file_get_contents('README.md');
$markdown = new Markdown($content, safeMode: true);
echo $markdown;
```

### Пользовательский комментарий
```php
// Безопасная обработка пользовательского ввода
$userComment = new Markdown(
    content: $_POST['comment'],
    safeMode: true, // Всегда true для пользовательского ввода!
    useExtra: false // Отключаем сложные функции для безопасности
);
```

### Комбинация с другими компонентами
```php
use OlegV\WallKit\Layout\Card;
use OlegV\WallKit\Content\Markdown\Markdown;

$card = new Card([
    new Markdown('# Заголовок карточки\n\nСодержимое в **Markdown**'),
]);
```

## ⚡ Производительность

Parsedown — одна из самых быстрых библиотек парсинга Markdown на PHP. Для дополнительной производительности:

1. **Кэширование** — кэшируйте скомпилированный HTML
2. **Минификация** — минифицируйте HTML вывод

## 🔗 Связанные компоненты

- **[Code](../Code/)** — для отдельных блоков кода с подсветкой
- **[Text](../Text/)** — для простого текста без Markdown
- **[Panel](../Panel/)** — для обертки Markdown контента

## 📚 Ресурсы

- [Parsedown GitHub](https://github.com/erusev/parsedown)
- [ParsedownExtra GitHub](https://github.com/erusev/parsedown-extra)
- [Markdown Guide](https://www.markdownguide.org/)

---

**Группа:** Content  
**Статус:** ✅ Готов  
**Зависимости:** `erusev/parsedown`  
**Требует JS:** Нет  
**Имеет CSS:** Да  
**Версия компонента:** 2.0.0  
**Создан:** 2024-01-01

[⬆️ Назад к оглавлению компонентов](../../README_INDEX.md#-контент-content)
```