# 🧩 Markdown

Компонент для рендеринга Markdown в HTML. Поддерживает базовый синтаксис Markdown с возможностью расширения.

## 📋 Свойства (Properties)

| Свойство | Тип | Обязательное | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `content` | `string` | Да | - | Markdown-контент для рендеринга |
| `allowHtml` | `bool` | Нет | `false` | Разрешить HTML-теги в Markdown (опасно!) |
| `safeMode` | `bool` | Нет | `true` | Безопасный режим (экранирует HTML) |
| `extensions` | `array` | Нет | все базовые | Включенные расширения Markdown |

## 🎯 Использование

### Базовый пример
```php
use WallKit\Content\Markdown\Markdown;

$markdown = new Markdown(
    content: '# Заголовок\n\nЭто **жирный** текст и [ссылка](https://example.com).'
);

echo $markdown;
```

### С пользовательскими расширениями
```php
$markdown = new Markdown(
    content: '~~Зачеркнутый~~ текст и таблица...',
    extensions: ['bold', 'italic', 'strikethrough', 'tables']
);
```

### С HTML (осторожно!)
```php
// Только если доверяете источнику контента
$markdown = new Markdown(
    content: '# Заголовок с <span style="color: red;">HTML</span>',
    allowHtml: true,
    safeMode: false
);
```

## 🎨 Поддерживаемые расширения

### Базовые (включены по умолчанию):
- `headers` — заголовки (#, ##, ###)
- `bold` — жирный текст (**текст**)
- `italic` — курсив (*текст*)
- `links` — ссылки ([текст](url))
- `images` — изображения (![alt](src))
- `lists` — списки (*, -, 1., 2.)
- `code` — код (`inline` и ```блоки```)
- `blockquotes` — цитаты (> текст)
- `horizontal` — горизонтальные линии (---)

### Дополнительные:
- `tables` — таблицы
- `strikethrough` — зачеркивание (~~текст~~)
- `autolinks` — автоматические ссылки
- `superscript` — верхний индекс
- `subscript` — нижний индекс
- `footnotes` — сноски

## ⚡ JavaScript Поведение

Компонент не требует JavaScript для базового рендеринга.  
Для подсветки синтаксиса кода можно использовать библиотеки типа Prism.js:

```javascript
// После загрузки компонента с кодом
document.querySelectorAll('.wallkit-markdown pre code').forEach((block) => {
    if (block.classList.contains('language-')) {
        Prism.highlightElement(block);
    }
});
```

## ⚠️ Безопасность

1. **По умолчанию включен `safeMode: true`** — все HTML теги экранируются
2. **`allowHtml: false` по умолчанию** — не разрешает HTML
3. **Используйте `allowHtml: true` только для доверенного контента**
4. **Всегда валидируйте входящий Markdown**

## 🧪 Примеры

```php
// Чтение из файла
$content = file_get_contents('README.md');
$markdown = new Markdown(content: $content);

// Динамическое содержимое
$postContent = $_POST['content'] ?? '';
$markdown = new Markdown(
    content: $postContent,
    safeMode: true, // Всегда для пользовательского ввода!
    allowHtml: false
);

// Минимальный набор
$simple = new Markdown(
    content: '**Важно**: Прочтите это!',
    extensions: ['bold'] // Только жирный текст
);
```

## 🔗 Связанные компоненты

- **[Code](../Code/)** — для блоков кода с подсветкой
- **[Headings](../Headings/)** — для отдельных заголовков
- **[Text](../Text/)** — для простого текста

## ⚠️ Ограничения

1. Не поддерживает все возможности CommonMark/GFM
2. Нет вложенных списков глубиной > 1
3. Нет сложных таблиц с объединением ячеек
4. Производительность: для больших документов лучше использовать библиотеки

---

**Группа:** Content  
**Статус:** ✅ Готов  
**Требует JS:** Нет (опционально для подсветки синтаксиса)  
**Имеет CSS:** Да  
**Версия компонента:** 1.0.0  
**Создан:** 2024-01-01

[⬆️ Назад к оглавлению компонентов](../../README_INDEX.md#-контент-content)