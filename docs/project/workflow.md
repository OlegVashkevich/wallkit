# Workflow для разработки WallKit

## 🔄 Общий процесс

1. **Выбор итерации** — работаем строго по порядку из `tasklist.md`
2. **Согласование решения** — перед началом итерации показываем предполагаемый код
3. **Реализация** — пишем код, следуя `concept.md` и `conventions.md`
4. **Подтверждение** — после реализации ждём подтверждения
5. **Обновление прогресса** — отмечаем задачи в `tasklist.md`
6. **Согласование перехода** — получаем разрешение на следующую итерацию
7. **Коммит** — фиксируем изменения в репозитории

---

## 📌 Правила выполнения

### 1. Начинаем только с согласованной итерации
- Открываем `tasklist.md`
- Выбираем следующую итерацию со статусом `⏳ Ожидание`
- Меняем статус на `🟡 В работе`
- Не перескакиваем между итерациями

### 2. Перед реализацией — показываем код
Для каждой задачи в итерации:
- Пишем примерный код (класс, шаблон, CSS, JS)
- Показываем его для согласования
- Вносим правки, если нужно
- Только после одобрения — пишем финальную версию

### 3. Реализуем по порядку внутри итерации
Для каждого компонента:
1. `Component.php` — PHP-класс
2. `template.php` — HTML-шаблон
3. `style.css` — стили
4. `script.js` — JS (если нужно)

### 4. Следуем стандартам
- Читаем `concept.md` перед каждой задачей
- Соблюдаем правила из `conventions.md`
- Пишем тесты параллельно с кодом
- Документируем изменения сразу

### 5. После завершения итерации
- Меняем статус на `✅ Завершено`
- Обновляем дату окончания
- Пишем коммит с понятным сообщением
- Ждём подтверждения перед переходом к следующей итерации

---

## 💬 Формат согласования

```
Итерация X: [Название]
Задача: [Описание]

Предлагаемый код:

[Код]

✅ Соответствует concept.md
✅ Соответствует conventions.md
✅ Прост и минималистичен

Готов реализовать?
```

---

## 🚀 Пример выполнения итерации

1. **Выбор:** Итерация 2 — Типографика и основы
2. **Согласование:** Показываем код для `Headings.php`
3. **Реализация:** Пишем все 4 файла для Headings
4. **Тесты:** Пишем тесты для Headings
5. **Повторяем** для Text, Link, Divider, Container
6. **Обновляем** `tasklist.md`
7. **Коммитим:** `git commit -m "Итерация 2: добавлены типографика и основы"`
8. **Переход:** Ждём подтверждения для Итерации 3

---

## ⚠️ Важные правила

- ❌ Не начинать без согласования кода
- ❌ Не менять порядок итераций
- ❌ Не пропускать тесты
- ❌ Не нарушать KISS
- ✅ Всегда обновлять `tasklist.md`
- ✅ Всегда коммитить после итерации
- ✅ Всегда ждать подтверждения перед переходом

---

## 📎 Ссылки

- [concept.md](./concept.md) — техническое видение
- [conventions.md](./conventions.md) — правила кода
- [tasklist.md](./tasklist.md) — план разработки

---

*Workflow создан для обеспечения последовательной, контролируемой и качественной разработки WallKit.*
