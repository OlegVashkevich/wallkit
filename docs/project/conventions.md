# Правила разработки для WallKit

## 📋 Общая информация

Этот документ содержит правила разработки для WallKit. Основные принципы и архитектурные решения описаны в [concept.md](./concept.md). Здесь только практические правила для написания кода.

## 🎯 Основные принципы (ссылка на concept.md)

1. **KISS всегда** – простота важнее универсальности. Один компонент = одна ответственность.
2. **PHP-first** - серверный рендеринг по умолчанию
3. **Типизация строгая** – все свойства readonly, все типы явные, union-типы для nullable.
4. **Минимализм** - только необходимое
5. **Следуй концепции** – все решения должны соответствовать документу `concept.md`.

## 📁 Структура компонента
```
Component/
├── Component.php # Класс компонента (readonly)
├── template.php # HTML-шаблон (чистый PHP + HTML)
├── style.css # Стили (опционально, только CSS-переменные из Base)
└── script.js # Только если нужен JS (опционально, ES6, без фреймворков)
```

**Правила:**
- Одна папка = один компонент
- Всегда 4 файла (даже если CSS/JS пустые)
- Имена папок: PascalCase (`Button`, `TextInput`)


## 📝 Правила написания кода

### PHP классы компонентов

```php
<?php

declare(strict_types=1); // ОБЯЗАТЕЛЬНО

namespace WallKit\Components\Button;

use WallKit\Base\Base;
use OlegV\Traits\WithHelpers; // Используем трейты Brick

/**
 * Компонент кнопки
 * @property string $text Текст кнопки
 * @property string $variant Вариант (primary, secondary)
 * @property string|null $icon Иконка emoji
 */
readonly class Button extends Base // ВСЕГДА readonly
{
    use WithHelpers; // Подключаем нужные трейты
    
    public function __construct(
        public string $text,                     // Тип + имя
        public string $variant = 'primary',      // Дефолтное значение
        public ?string $icon = null,             // Nullable для опциональных
    ) {
        // Валидация ТОЛЬКО если критично
        if (empty(trim($this->text))) {
            throw new \InvalidArgumentException('Текст кнопки не может быть пустым');
        }
        
        parent::__construct(); // ВСЕГДА вызываем
    }
    
    // Дополнительные методы ТОЛЬКО если нужны
    public function getUppercaseText(): string
    {
        return mb_strtoupper($this->text);
    }
}
```

**Обязательные правила:**
1. `declare(strict_types=1);` всегда
2. Класс всегда `readonly`
3. Все свойства публичные в конструкторе
4. Всегда `parent::__construct()` в конце конструктора
5. Документация `@property` для каждого свойства
6. Имена параметров в конструкторе такие же как имена свойств
7. **Наследование** – все компоненты наследуют `Base`.
8. **Исключения** – `InvalidArgumentException` для ошибок валидации.

### Шаблоны (template.php)

```php
<?php
/** @var ComponentName $this */ // УКАЗЫВАТЬ ТИП ДЛЯ IDE
use WallKit\Components\Button\Button;

// ТОЛЬКО PHP и HTML, никакого логики
?>
<button class="wallkit-button wallkit-button--<?= $this->e($this->variant) ?>">
    <?php if ($this->icon): ?>
        <span class="wallkit-button__icon"><?= $this->e($this->icon) ?></span>
    <?php endif; ?>
    
    <span class="wallkit-button__text">
        <?= $this->e($this->text) ?>
    </span>
</button>
```

**Правила:**
1. Всегда указывать `@var` вверху файла
2. Использовать `$this->e()` для экранирования ВСЕХ выводов
3. Минимум логики в шаблоне
4. CSS классы с префиксом `wallkit-`

### CSS (style.css)

```css
/* Используем переменные из Base */
.wallkit-button {
    background-color: var(--wk-color-primary);
    color: white;
    font-family: var(--wk-font-family);
}

/* BEM-нотация опционально */
.wallkit-button__icon {
    margin-right: var(--wk-spacing-2);
}

/* Модификаторы через -- */
.wallkit-button--primary {
    background-color: var(--wk-color-primary);
}

.wallkit-button--secondary {
    background-color: var(--wk-color-secondary);
}
```

**Правила:**
1. Использовать CSS переменные из `Base/style.css`
2. Префикс `wallkit-` для всех классов
3. Используй `--wk-*` переменные.
4. Без каскада – стили только для данного компонента.

### JavaScript (script.js)

```javascript
// Прогрессивное улучшение - код работает если JS доступен
document.addEventListener('DOMContentLoaded', function() {
    // Селекторы по CSS классам компонента
    document.querySelectorAll('.wallkit-button').forEach(button => {
        button.addEventListener('click', function(e) {
            // Логика
        });
    });
});
```

**Правила:**
1. Только нативный JS (ES6+)
2. Минимум JS – только для интерактивности.
3. Селекторы по CSS классам компонента
4. Обработка ошибок
5. Прогрессивное улучшение
6. Data-атрибуты – для связи с JS: data-action="...".

## 🔧 Конфигурация и зависимости

### Использование Brick UI
```php
use OlegV\Brick;                    // Базовый класс
use OlegV\Traits\WithCache;         // Трейты
use OlegV\Traits\WithHelpers;
use OlegV\BrickManager;             // Менеджер
use OlegV\Assets\FileAssetRenderer; // Ассеты
```

**Правила:**
1. Наследуем `OlegV\Brick` через наш `Base`
2. Используем трейты Brick напрямую
3. BrickManager используем напрямую
4. НЕ создаем обертки без необходимости

### Валидация и ошибки

```php
// ТОЛЬКО критичная валидация
if (empty(trim($value))) {
    throw new \InvalidArgumentException('Значение не может быть пустым');
}

// Понятные сообщения
throw new \RuntimeException(
    'Не удалось отрендерить компонент ' . static::class . 
    '. Файл: ' . $templatePath
);
```

**Правила:**
1. Валидация только в конструкторе
2. Только для критичных случаев
3. Понятные сообщения об ошибках
4. Исключения вместо логирования

## 🧪 Тестирование

### Структура тестов
```
tests/
├── Unit/
│   └── Components/
│       └── ButtonTest.php
└── bootstrap.php
```

### Пример теста
```php
class ButtonTest extends TestCase
{
    public function testButtonRendersText(): void
    {
        $button = new Button('Нажми меня');
        $html = (string) $button;
        
        $this->assertStringContainsString('Нажми меня', $html);
        $this->assertStringContainsString('wallkit-button', $html);
    }
}
```

## Версионирование
- Семантическое версионирование (SemVer)
- CHANGELOG.md для всех изменений
- Git теги для релизов

## Коммиты и документация
1. DocBlock обязателен – для всех классов и публичных методов.
2. README.md в папке – если компонент сложный.
3. Примеры в комментариях – как в concept.md.

## ✅ Чеклист для нового компонента

1. [ ] Создана папка `ComponentName/` в нужной группе
2. [ ] Созданы 4 файла с правильными именами
3. [ ] Класс `readonly` extends `Base`
4. [ ] `declare(strict_types=1)` в начале
5. [ ] Документация `@property` для всех свойств
6. [ ] Валидация в конструкторе если нужно
7. [ ] `parent::__construct()` вызван
8. [ ] Шаблон использует `$this->e()` для вывода
9. [ ] CSS использует переменные и префикс `wallkit-`
10. [ ] JS не больше 50 строк, прогрессивное улучшение
11. [ ] Тесты созданы (опционально)

---

## 🚫 Что НЕ делать

1. ❌ НЕ создавать сеттеры (компоненты иммутабельные)
2. ❌ НЕ добавлять бизнес-логику в шаблоны
3. ❌ НЕ использовать глобальные переменные
4. ❌ НЕ смешивать CSS/JS между компонентами
5. ❌ НЕ создавать сложные иерархии наследования (>3 уровней)
6. ❌ НЕ делать компоненты зависимыми от фреймворков
7. ❌ НЕ логировать внутри библиотеки (только исключения)
8. ❌ НЕ использовать сборщики (Webpack, Vite)
9. ❌ НЕ использовать CSS препроцессоры
10. ❌ НЕ усложнять без необходимости

---

Главное: Если есть сомнения – смотри [concept.md](./concept.md). Если там нет ответа – выбирай самое простое решение.