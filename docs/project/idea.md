# Идея проекта: Библиотека компонентов для построения пользовательских интерфейсов на PHP

## 🎯 Название
**WallKit** — UI-библиотека на PHP с SSR-подходом и иммутабельными компонентами.

## 🧠 Основная концепция
Создание современной, типобезопасной и производительной библиотеки UI-компонентов, полностью построенной на PHP, с акцентом на серверный рендеринг (SSR) и иммутабельность компонентов.

## ✨ Ключевые принципы
- **PHP-first** — компоненты реализованы как PHP-классы
- **HTML-first** — чистый HTML + CSS, минимальный JavaScript
- **SSR по умолчанию** — рендеринг на стороне сервера
- **Иммутабельность** — предсказуемость и безопасность компонентов
- **Полная типизация** — strict_types=1, статический анализ через PHPStan
- **БЭМ + CSS-переменные** — модульные и настраиваемые стили

## 🧩 Группы компонентов
Компоненты сгруппированы по функциональному назначению:

### 1. Формы
- Input, Textarea, Select, Checkbox, Radio, Button, Form, Field, DatePicker, FileUpload

### 2. Навигация
- Menu, Sidebar, Breadcrumb, Pagination, Tabs, Stepper, Dropdown

### 3. Данные
- Table, DataGrid, Card, List, Tree, Accordion, Carousel

### 4. Обратная связь
- Modal, Dialog, Alert, Notification, Tooltip, Popover, ProgressBar, Spinner

### 5. Контент
- **Figure** — для изображений с подписью
- **Code** — блоки кода с подсветкой синтаксиса
- Typography (Headings, Text, Link), Divider, Panel, Badge

### 6. Компоновка
- Container, Grid, Flex, Stack, SplitPanel, Dashboard, AppLayout

### 7. Служебные
- Portal, Overlay, Backdrop, Skeleton, EmptyState

## 🏗️ Архитектура
Каждый компонент состоит из 4 файлов:
1. `Component.php` — PHP-класс компонента
2. `template.php` — HTML-шаблон
3. `style.css` — стили компонента
4. `script.js` — опциональный JavaScript

### Ядро библиотеки:
- **Brick** — базовый иммутабельный класс компонента
- **BrickManager** — центральный менеджер для кэширования, регистрации и рендеринга компонентов
- **Трейты для расширения** — WithCache, WithHelpers, WithInheritance, WithStrictHelpers

## 🚀 Установка и использование
```bash
composer require olegv/wallkit
```

```php
use OlegV\WallKit\Form\Input\Input;
$input = new Input(name: 'username', label: 'Имя пользователя');
echo $input;
```

## 🎨 Кастомизация
Библиотека использует CSS-переменные для лёгкой настройки:
```css
:root {
    --wk-color-primary: #3b82f6;
    --wk-spacing-unit: 0.5rem;
    --wk-font-family: sans-serif;
}
```

## 🧪 Тестирование и качество
- 100% покрытие тестами (PHPUnit)
- Полный статический анализ через PHPStan
- Интеграционные и E2E-тесты
- Документация в коде и отдельные MD-файлы

## 📦 Формат распространения
- Единый Composer-пакет
- Исходный код без минификации
- Встроенная интерактивная документация на PHP
- Совместимость с любым PHP-фреймворком

## 🧭 Философия
**Простота, типизация, SSR.**  
Создание UI без тяжёлых JS-фреймворков, с упором на производительность, безопасность и разработку на PHP.

## 🤝 Целевая аудитория
- PHP-разработчики, предпочитающие серверный рендеринг
- Команды, нуждающиеся в типобезопасных UI-компонентах
- Проекты, где важна производительность и минимальный JavaScript
