# Руководство по стилизации WallKit

## 🎯 Принципы стилизации

| Принцип | Описание | Пример |
|---------|----------|--------|
| **CSS-переменные** | Единый источник истины для тем | `--wk-color-primary` |
| **Иммутабельность** | Стили не переопределяют друг друга | БЭМ-нотация |
| **Прогрессивное улучшение** | Базовая функциональность без CSS | Семантический HTML |
| **Доступность** | WCAG 2.1 AA+ стандарт | Контраст 4.5:1 |
| **Производительность** | Минимум CSS, нет препроцессоров | Нативные переменные |

## 🎨 Система дизайна

### Цветовая палитра

| Переменная | Значение по умолчанию | Назначение |
|------------|----------------------|------------|
| `--wk-color-primary` | `#3b82f6` | Основной цвет (синий) |
| `--wk-color-secondary` | `#6b7280` | Вторичный цвет (серый) |
| `--wk-color-success` | `#10b981` | Успех (зеленый) |
| `--wk-color-danger` | `#ef4444` | Ошибка (красный) |
| `--wk-color-warning` | `#f59e0b` | Предупреждение (оранжевый) |
| `--wk-color-info` | `#0ea5e9` | Информация (голубой) |

#### Градации серого
```
--wk-color-gray-50:  #f9fafb
--wk-color-gray-100: #f3f4f6
--wk-color-gray-200: #e5e7eb
--wk-color-gray-300: #d1d5db
--wk-color-gray-400: #9ca3af
--wk-color-gray-500: #6b7280
--wk-color-gray-600: #4b5563
--wk-color-gray-700: #374151
--wk-color-gray-800: #1f2937
--wk-color-gray-900: #111827
```

### Типографика

| Переменная | Значение | Назначение |
|------------|----------|------------|
| `--wk-font-family` | `system-ui, -apple-system, sans-serif` | Основной шрифт |
| `--wk-font-size-xs` | `0.75rem` (12px) | Очень мелкий текст |
| `--wk-font-size-sm` | `0.875rem` (14px) | Мелкий текст |
| `--wk-font-size-base` | `1rem` (16px) | Базовый размер |
| `--wk-font-size-lg` | `1.125rem` (18px) | Крупный текст |
| `--wk-font-size-xl` | `1.25rem` (20px) | Очень крупный |
| `--wk-line-height-sm` | `1.25` | Компактный межстрочный |
| `--wk-line-height-base` | `1.5` | Стандартный межстрочный |
| `--wk-line-height-lg` | `1.75` | Растянутый межстрочный |

### Отступы (Spacing)

| Переменная | Значение | Эквивалент |
|------------|----------|------------|
| `--wk-spacing-1` | `0.25rem` | 4px |
| `--wk-spacing-2` | `0.5rem` | 8px |
| `--wk-spacing-2-5` | `0.625rem` | 10px |
| `--wk-spacing-3` | `0.75rem` | 12px |
| `--wk-spacing-4` | `1rem` | 16px |
| `--wk-spacing-5` | `1.25rem` | 20px |
| `--wk-spacing-6` | `1.5rem` | 24px |
| `--wk-spacing-8` | `2rem` | 32px |

### Радиусы скругления

| Переменная | Значение | Использование |
|------------|----------|---------------|
| `--wk-radius-none` | `0` | Квадратные углы |
| `--wk-radius-sm` | `0.125rem` (2px) | Мелкие скругления |
| `--wk-radius-base` | `0.25rem` (4px) | Стандартные |
| `--wk-radius-md` | `0.375rem` (6px) | Средние |
| `--wk-radius-lg` | `0.5rem` (8px) | Крупные |
| `--wk-radius-xl` | `0.75rem` (12px) | Очень крупные |
| `--wk-radius-full` | `9999px` | Полное скругление |

### Тени (Box Shadows)

| Переменная | Значение | Использование |
|------------|----------|---------------|
| `--wk-shadow-sm` | `0 1px 2px 0 rgba(0,0,0,0.05)` | Легкая тень |
| `--wk-shadow-base` | `0 1px 3px 0 rgba(0,0,0,0.1)` | Стандартная |
| `--wk-shadow-md` | `0 4px 6px -1px rgba(0,0,0,0.1)` | Средняя |
| `--wk-shadow-lg` | `0 10px 15px -3px rgba(0,0,0,0.1)` | Крупная |
| `--wk-shadow-outline-blue` | `0 0 0 3px rgba(59,130,246,0.5)` | Синий контур |
| `--wk-shadow-outline-red` | `0 0 0 3px rgba(239,68,68,0.5)` | Красный контур |

### Анимации и переходы

| Переменная | Значение | Описание |
|------------|----------|----------|
| `--wk-transition-fast` | `150ms ease-in-out` | Быстрый переход |
| `--wk-transition-default` | `200ms ease-in-out` | Стандартный переход |
| `--wk-transition-slow` | `300ms ease-in-out` | Медленный переход |

## 🏗️ Архитектура стилей

### Структура файлов
```
src/
├── Base/
│   └── style.css          # Корневые CSS-переменные
└── [ComponentGroup]/
    └── [Component]/
        └── style.css      # Стили компонента
```

### Правила именования классов

| Паттерн | Пример | Назначение |
|---------|--------|------------|
| `wallkit-[компонент]` | `.wallkit-button` | Базовый класс компонента |
| `wallkit-[компонент]__[элемент]` | `.wallkit-button__icon` | Элемент компонента (БЭМ) |
| `wallkit-[компонент]--[модификатор]` | `.wallkit-button--primary` | Модификатор состояния |
| `wallkit-[компонент]--[размер]` | `.wallkit-button--sm` | Модификатор размера |

### Пример компонента
```css
/* Базовый класс */
.wallkit-button {
    /* Используем переменные */
    background-color: var(--wk-color-primary);
    padding: var(--wk-spacing-3) var(--wk-spacing-4);
    border-radius: var(--wk-radius-base);
}

/* Элемент */
.wallkit-button__icon {
    margin-right: var(--wk-spacing-2);
}

/* Модификатор состояния */
.wallkit-button--disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

/* Модификатор размера */
.wallkit-button--sm {
    padding: var(--wk-spacing-2) var(--wk-spacing-3);
    font-size: var(--wk-font-size-sm);
}
```

## 🎭 Кастомизация темы

### Переопределение переменных
```css
/* В вашем проекте: styles.css */
:root {
    /* Переопределяем цвета */
    --wk-color-primary: #8b5cf6;
    --wk-color-secondary: #a78bfa;
    
    /* Изменяем отступы */
    --wk-spacing-3: 0.875rem;
    
    /* Настраиваем шрифты */
    --wk-font-family: 'Inter', sans-serif;
}
```

### Создание темной темы
```css
[data-theme="dark"] {
    --wk-color-primary: #8b5cf6;
    --wk-color-gray-900: #ffffff;
    --wk-color-gray-800: #f3f4f6;
    --wk-color-gray-100: #111827;
    background-color: #0f172a;
    color: white;
}
```

## 📱 Адаптивность

### Breakpoints (через контейнерные запросы)
```css
/* Базовые стили для мобильных */
.wallkit-container {
    width: 100%;
}

/* Планшеты (768px+) */
@container (min-width: 48rem) {
    .wallkit-container {
        max-width: 48rem;
    }
}

/* Десктопы (1024px+) */
@container (min-width: 64rem) {
    .wallkit-container {
        max-width: 64rem;
    }
}
```

### Mobile-first подход
```css
/* Базовые стили (мобильные) */
.wallkit-grid {
    display: flex;
    flex-direction: column;
    gap: var(--wk-spacing-4);
}

/* Десктоп (1024px+) */
@container (min-width: 64rem) {
    .wallkit-grid {
        flex-direction: row;
        gap: var(--wk-spacing-6);
    }
}
```

## ♿ Доступность (a11y)

### Цветовой контраст
| Элемент | Минимальный контраст | Пример |
|---------|---------------------|--------|
| Текст (обычный) | 4.5:1 | `#374151` на `#ffffff` |
| Текст (крупный) | 3:1 | `18px+`, жирный |
| UI компоненты | 3:1 | Кнопки, инпуты |
| Графика | 3:1 | Иконки, чарты |

### Фокус-индикаторы
```css
.wallkit-button:focus-visible {
    outline: 2px solid var(--wk-color-primary);
    outline-offset: 2px;
}

.wallkit-input__field:focus-visible {
    box-shadow: var(--wk-shadow-outline-blue);
}
```

## 🧪 Тестирование стилей

### Проверка контраста
```javascript
// Использование DevTools или инструментов типа axe
// Минимальные требования WCAG 2.1 AA
```

### Кросс-браузерная совместимость
| Браузер | Поддержка | Примечания |
|---------|-----------|------------|
| Chrome 90+ | ✅ Полная | CSS-переменные, Grid, Flexbox |
| Firefox 88+ | ✅ Полная | CSS-переменные, Container Queries |
| Safari 15+ | ✅ Полная | CSS-переменные, Gap |
| Edge 90+ | ✅ Полная | Аналогично Chrome |

## 📝 Code Style Guidelines

### Правила написания CSS
1. **Один компонент — один файл**
2. **Используйте только CSS-переменные**
3. **Избегайте !important**
4. **Минимум вложенности (max 2 уровня)**
5. **Комментируйте сложные решения**

### Порядок свойств
```css
/* 1. Позиционирование и бокс-модель */
position: absolute;
top: 0;
display: flex;
width: 100%;
padding: var(--wk-spacing-4);

/* 2. Типографика */
font-family: var(--wk-font-family);
font-size: var(--wk-font-size-base);
line-height: var(--wk-line-height-base);

/* 3. Визуальные стили */
background-color: var(--wk-color-primary);
border: 1px solid var(--wk-color-gray-300);
border-radius: var(--wk-radius-base);

/* 4. Анимации */
transition: var(--wk-transition-default);

/* 5. Прочее */
cursor: pointer;
```

## 🔧 Утилитарные классы (опционально)

| Класс | Назначение |
|-------|------------|
| `.wk-text-center` | `text-align: center` |
| `.wk-mt-4` | `margin-top: var(--wk-spacing-4)` |
| `.wk-p-4` | `padding: var(--wk-spacing-4)` |
| `.wk-flex` | `display: flex` |
| `.wk-gap-4` | `gap: var(--wk-spacing-4)` |

## 📚 Ресурсы

### Инструменты
- **Chrome DevTools** — аудит доступности, цветового контраста
- **axe DevTools** — автоматическое тестирование a11y
- **CSS Stats** — анализ размера и сложности CSS

### Стандарты
- [WCAG 2.1](https://www.w3.org/TR/WCAG21/)
- [CSS Custom Properties](https://www.w3.org/TR/css-variables-1/)
- [Container Queries](https://www.w3.org/TR/css-contain-3/)
